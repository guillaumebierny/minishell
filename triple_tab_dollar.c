#include "minishell.h"


char *s put_dollar(char *s,)
{
    if (s == '$')
    {
        if (((*s + 1) >= 9 && (*s + 1 ) <=  13) || (*s + 1) == 35)
        {
            add_arg_to_triple_tab():
        }
        while ()
        {

        }
    }
}

add_string_to_triple_tab(char ***tt, (void *)f(void))
{
    int i = -1;
    int j = -1;
    while (tt[++i])
    {
        while[++j]
        {
            
        }
    }
}
